{
  "name": "ai-mcp-server-git-commit",
  "version": "1.0.0",
  "description": "An MCP server that analyzes Git commits to determine team activity",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "simple-git": "^3.22.0",
    "dotenv": "^16.3.1"
  }
}
